{% extends 'base.html.twig' %}

{% block body %}
    {% for article in articles %}
        <div>

            <h2>{{ article.title }}</h2>
            <p>
                {{ article.content[1:150] }}
                {% if article.content|length > 150%} ... {% endif %}
            </p>

        </div>
    {% endfor %}
{% endblock %}
